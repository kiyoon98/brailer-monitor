# Runtime dependencies that are not supplied by the NVIDIA PyTorch base image.
# Ultralytics and supervision are installed with --no-deps in the Dockerfile so
# they use the headless OpenCV wheel instead of adding the GUI OpenCV package.
# NVIDIA PyTorch 25.08 was built against the NumPy 1.x ABI.
numpy==1.26.4
opencv-python-headless==4.11.0.86
scipy>=1.11.0
matplotlib>=3.3.0
pillow>=9.0.0
pyyaml>=5.3.1
requests>=2.23.0
psutil>=5.8.0
polars>=1.0.0
ultralytics-thop==2.0.20
defusedxml>=0.7.1

transformers==4.57.6
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32
